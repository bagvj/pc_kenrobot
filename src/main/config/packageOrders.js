module.exports = {
	/*
     * 包顺序。假如有3个包A、B、C，其中A没有定义order，B.order < C.order，那么排完之后是B、C、A
	 */
	// Arduino: -999,
	// Boxz: 10,
	// Intel: 20,
	Kenblock: -999,
	// UperEnergy: 40,

	standardPackage: "Kenblock",
}
